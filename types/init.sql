CREATE TABLE chars (
  name TEXT,
  rating CHAR(10),
  description VARCHAR(10)
);

CREATE TABLE integers (
  si SMALLINT,
  int INTEGER,
  bi BIGINT
);

CREATE TABLE floats (
  real REAL,
  double DOUBLE PRECISION
);

CREATE TABLE dates (
  date DATE,
  time TIME
);
